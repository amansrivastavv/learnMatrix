# LearnMatrix Project

This project is structured as a monorepo with the following components:

- **client/**: The Frontend application (Next.js).
- **server/**: The Backend application (To be initialized).

## Setup

### Client
Navigate to `client/` and run:
```bash
cd client
npm install
npm run dev
```

### Server
Navigate to `server/` and follow the backend-specific instructions.
